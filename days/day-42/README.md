# Task: Backup and Delete Azure Storage Blob Container

The Nautilus DevOps team is currently engaged in a cleanup process, focusing on removing unnecessary data and services from their Azure environment. As part of the migration process, several resources were created for one-time use only, necessitating a cleanup effort to optimize their Azure environment.

## Task Details

A private blob container named `devops-blob-428691458` already exists in the `southcentralus` region under storage account devopsst428691458.

1. Copy the contents of `devops-blob-428691458` blob container to the `/opt` directory on the `azure-client` host (the landing host once you load this lab).
1. Delete the blob container `devops-blob-428691458` from the storage account.



